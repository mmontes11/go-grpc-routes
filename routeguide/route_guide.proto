syntax = "proto3";

option go_package = "github.com/mmontes11/go-grpc-routes/routeguide";

package routeguide;

service RouteGuide {
    rpc GetFeature(Point) returns (Point) {}
}

message Point {
    int32 latitude = 1;
    int32 longitude = 2;
}